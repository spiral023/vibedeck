# X-Ingest Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein CLI-Script `npm run ingest:x -- <tweet-url>` holt Tweet-/Thread-Inhalte über die X-API (App-only Bearer Auth) und schreibt einen lesbaren Markdown-Dump + Roh-JSON-Cache, aus dem Claude Knowledge-Artikel erstellt.

**Architecture:** Drei Dateien mit klaren Grenzen: `scripts/lib/x-ingest.mjs` (reine, testbare Logik — Parsing, Extraktion, Dump-Formatierung), `scripts/lib/x-client.mjs` (dünne X-API-Aufrufe via `twitter-api-v2`), `scripts/ingest-x.mjs` (CLI-Orchestrierung, Cache, Datei-I/O, Fehlerbehandlung). Reine Logik wird per Vitest getestet; Netzwerkcode per echtem Lauf verifiziert.

**Tech Stack:** Node 24 (ESM `.mjs`, `process.loadEnvFile`), `twitter-api-v2` (devDependency), Vitest (vorhanden).

## Global Constraints

- Node ≥ 20.6 für `process.loadEnvFile` (lokal v24 vorhanden).
- `twitter-api-v2` wird als **devDependency** installiert (nie im App-Bundle).
- Nur `X_BEARER_TOKEN` wird gelesen (App-only, read-only). Consumer Key/Secret werden NICHT verwendet.
- `.env.local` und `scripts/.ingest/` sind gitignored.
- Pay-per-use: minimale Requests — ein Call pro Tweet, lokaler Cache, Thread-Search auf **eine Seite** (max 100) begrenzt, Rückwärts-Walk gedeckelt auf `--max-thread` (Default 50). Kein Auto-Retry bei 429.
- Scripts folgen dem Stil von `scripts/generate-search-index.mjs` (natives Node, ESM, keine Framework-Imports).
- Vitest-Testdateien müssen `.ts` sein (Config-Glob `**/*.{test,spec}.{ts,tsx}`); sie importieren aus den `.mjs`-Modulen.

---

## File Structure

| Datei | Verantwortung |
| --- | --- |
| `scripts/lib/x-ingest.mjs` | Reine Funktionen: `parseArgs`, `parseTweetId`, `extractTweetText`, `extractLinks`, `extractMedia`, `isThreadStart`, `orderThreadChronologically`, `formatDump`. Kein Netzwerk, kein fs. |
| `scripts/lib/x-ingest.test.ts` | Vitest-Unit-Tests für alle reinen Funktionen. |
| `scripts/lib/x-client.mjs` | X-API-Client: `createClient`, Feld-Konstanten, `fetchTweet`, `resolveThreadForward`, `resolveThreadBackward`. |
| `scripts/ingest-x.mjs` | CLI-Entry: Arg-/Env-Handling, Cache, Orchestrierung, Datei-Output, Konsolenausgabe, Fehlermeldungen. |
| `.env.example` | Doku der erwarteten Variable `X_BEARER_TOKEN` (committed). |
| `.gitignore` | Ergänzung `scripts/.ingest/`. |
| `package.json` | Neues Script `ingest:x`, neue devDependency. |
| `CLAUDE.md` | Abschnitt „Knowledge-Artikel aus X-Posts". |

---

## Task 1: Scaffolding + CLI-Skeleton

**Files:**
- Modify: `package.json` (devDependency + `ingest:x` Script)
- Modify: `.gitignore` (add `scripts/.ingest/`)
- Create: `.env.example`
- Create: `scripts/ingest-x.mjs` (nur Usage-Ausgabe)

**Interfaces:**
- Produces: lauffähiges `npm run ingest:x`, das ohne Argument eine Usage-Meldung ausgibt und mit Exit-Code 1 endet.

- [ ] **Step 1: `twitter-api-v2` als devDependency installieren**

Run:
```bash
npm install --save-dev twitter-api-v2
```
Expected: `package.json` → `devDependencies` enthält `"twitter-api-v2"`, Exit 0.

- [ ] **Step 2: npm-Script `ingest:x` ergänzen**

In `package.json`, `scripts`-Block, nach `content:backfill-added-date` einfügen:
```json
    "ingest:x": "node scripts/ingest-x.mjs",
```

- [ ] **Step 3: `.gitignore` ergänzen**

Am Ende von `.gitignore` anhängen:
```
# X-Ingest cache/output
scripts/.ingest/
```

- [ ] **Step 4: `.env.example` anlegen**

Create `.env.example`:
```
# X (Twitter) API — App-only Bearer Token für scripts/ingest-x.mjs
# X Developer Portal → App "vibedeck-ingest" → Keys and tokens → Bearer Token
X_BEARER_TOKEN=
```

- [ ] **Step 5: CLI-Skeleton mit Usage anlegen**

Create `scripts/ingest-x.mjs`:
```javascript
function usage() {
  console.log(
    'Usage: npm run ingest:x -- <tweet-url-oder-id> [--thread] [--force] [--max-thread <n>]',
  );
}

const input = process.argv.slice(2).find((a) => !a.startsWith('--'));
if (!input) {
  usage();
  process.exit(1);
}

console.log('not-yet-implemented');
```

- [ ] **Step 6: Verifizieren, dass Usage erscheint**

Run:
```bash
npm run ingest:x
```
Expected: gibt die Usage-Zeile aus, Exit-Code 1 (npm meldet Fehler wegen exit 1).

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json .gitignore .env.example scripts/ingest-x.mjs
git commit -m "chore(ingest): scaffold X-ingest CLI (dep, script, gitignore, env)"
```

---

## Task 2: Reine Logik — Arg- und ID-Parsing

**Files:**
- Create: `scripts/lib/x-ingest.mjs`
- Create: `scripts/lib/x-ingest.test.ts`

**Interfaces:**
- Produces:
  - `parseArgs(argv: string[]): { input?: string, thread: boolean, force: boolean, maxThread: number }`
  - `parseTweetId(input: string): string` — wirft `Error` bei ungültiger Eingabe.

- [ ] **Step 1: Failing Tests schreiben**

Create `scripts/lib/x-ingest.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { parseArgs, parseTweetId } from './x-ingest.mjs';

describe('parseTweetId', () => {
  it('extrahiert die ID aus einer x.com-URL', () => {
    expect(parseTweetId('https://x.com/trq212/status/2073100352921215386')).toBe('2073100352921215386');
  });
  it('extrahiert die ID aus einer twitter.com-URL', () => {
    expect(parseTweetId('https://twitter.com/user/status/123')).toBe('123');
  });
  it('ignoriert Query-Parameter', () => {
    expect(parseTweetId('https://x.com/user/status/456?s=20&t=abc')).toBe('456');
  });
  it('akzeptiert eine nackte numerische ID', () => {
    expect(parseTweetId('789')).toBe('789');
  });
  it('wirft bei ungültiger Eingabe', () => {
    expect(() => parseTweetId('https://x.com/user')).toThrow();
    expect(() => parseTweetId('')).toThrow();
  });
});

describe('parseArgs', () => {
  it('liest positionalen Input', () => {
    expect(parseArgs(['https://x.com/u/status/1']).input).toBe('https://x.com/u/status/1');
  });
  it('erkennt Flags und Default maxThread', () => {
    const a = parseArgs(['1', '--thread', '--force']);
    expect(a.thread).toBe(true);
    expect(a.force).toBe(true);
    expect(a.maxThread).toBe(50);
  });
  it('liest --max-thread mit Wert', () => {
    expect(parseArgs(['1', '--max-thread', '10']).maxThread).toBe(10);
  });
});
```

- [ ] **Step 2: Tests laufen lassen (müssen fehlschlagen)**

Run:
```bash
npx vitest run scripts/lib/x-ingest.test.ts
```
Expected: FAIL — Modul `./x-ingest.mjs` existiert nicht / Funktionen undefiniert.

- [ ] **Step 3: Minimale Implementierung**

Create `scripts/lib/x-ingest.mjs`:
```javascript
export function parseArgs(argv) {
  const args = { input: undefined, thread: false, force: false, maxThread: 50 };
  for (let i = 0; i < argv.length; i += 1) {
    const a = argv[i];
    if (a === '--thread') args.thread = true;
    else if (a === '--force') args.force = true;
    else if (a === '--max-thread') {
      const n = Number.parseInt(argv[i + 1], 10);
      args.maxThread = Number.isFinite(n) && n > 0 ? n : 50;
      i += 1;
    } else if (!a.startsWith('--') && args.input === undefined) {
      args.input = a;
    }
  }
  return args;
}

export function parseTweetId(input) {
  const trimmed = (input ?? '').trim();
  if (/^\d+$/.test(trimmed)) return trimmed;
  const match = trimmed.match(/(?:twitter\.com|x\.com)\/[^/]+\/status\/(\d+)/i);
  if (match) return match[1];
  throw new Error(
    `Konnte keine Tweet-ID aus "${input}" lesen. Erwartet: Tweet-URL oder numerische ID.`,
  );
}
```

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run:
```bash
npx vitest run scripts/lib/x-ingest.test.ts
```
Expected: PASS (alle Tests grün).

- [ ] **Step 5: Commit**

```bash
git add scripts/lib/x-ingest.mjs scripts/lib/x-ingest.test.ts
git commit -m "feat(ingest): add arg + tweet-id parsing with tests"
```

---

## Task 3: Reine Logik — Extraktion + Dump-Formatierung

**Files:**
- Modify: `scripts/lib/x-ingest.mjs`
- Modify: `scripts/lib/x-ingest.test.ts`

**Interfaces:**
- Consumes: nichts Neues.
- Produces:
  - `extractTweetText(tweet): string` — `note_tweet.text` bevorzugt, sonst `text`, sonst `''`.
  - `extractLinks(tweet): Array<{ url: string, display: string }>` — dedupliziert, expanded URLs.
  - `extractMedia(mediaList): Array<{ type: string, alt: string, url: string }>`.
  - `isThreadStart(tweet): boolean` — `tweet.id === tweet.conversation_id`.
  - `orderThreadChronologically(tweets): tweet[]` — aufsteigend nach `created_at`.
  - `formatDump({ tweet, author, media, thread }): string` — Markdown-Dump.

- [ ] **Step 1: Failing Tests ergänzen**

An `scripts/lib/x-ingest.test.ts` anhängen (Import-Zeile oben erweitern):
```typescript
import {
  parseArgs,
  parseTweetId,
  extractTweetText,
  extractLinks,
  extractMedia,
  isThreadStart,
  orderThreadChronologically,
  formatDump,
} from './x-ingest.mjs';

describe('extractTweetText', () => {
  it('bevorzugt note_tweet über text', () => {
    expect(extractTweetText({ text: 'kurz', note_tweet: { text: 'langer volltext' } })).toBe('langer volltext');
  });
  it('fällt auf text zurück', () => {
    expect(extractTweetText({ text: 'nur text' })).toBe('nur text');
  });
  it('gibt leeren String ohne Inhalt', () => {
    expect(extractTweetText({})).toBe('');
  });
});

describe('extractLinks', () => {
  it('nimmt expanded_url und dedupliziert', () => {
    const tweet = {
      entities: {
        urls: [
          { url: 'https://t.co/a', expanded_url: 'https://example.com', display_url: 'example.com' },
          { url: 'https://t.co/b', expanded_url: 'https://example.com', display_url: 'example.com' },
        ],
      },
    };
    expect(extractLinks(tweet)).toEqual([{ url: 'https://example.com', display: 'example.com' }]);
  });
  it('bevorzugt note_tweet.entities', () => {
    const tweet = {
      entities: { urls: [{ expanded_url: 'https://old.com' }] },
      note_tweet: { entities: { urls: [{ expanded_url: 'https://new.com', display_url: 'new.com' }] } },
    };
    expect(extractLinks(tweet)).toEqual([{ url: 'https://new.com', display: 'new.com' }]);
  });
  it('gibt leeres Array ohne urls', () => {
    expect(extractLinks({})).toEqual([]);
  });
});

describe('extractMedia', () => {
  it('mappt Typ, Alt-Text und URL', () => {
    const media = [{ type: 'photo', alt_text: 'ein Bild', url: 'https://img/1.jpg' }];
    expect(extractMedia(media)).toEqual([{ type: 'photo', alt: 'ein Bild', url: 'https://img/1.jpg' }]);
  });
  it('nutzt preview_image_url wenn url fehlt', () => {
    expect(extractMedia([{ type: 'video', preview_image_url: 'https://img/p.jpg' }])).toEqual([
      { type: 'video', alt: '', url: 'https://img/p.jpg' },
    ]);
  });
  it('gibt leeres Array für Nicht-Array', () => {
    expect(extractMedia(undefined)).toEqual([]);
  });
});

describe('isThreadStart', () => {
  it('true wenn id === conversation_id', () => {
    expect(isThreadStart({ id: '5', conversation_id: '5' })).toBe(true);
  });
  it('false wenn abweichend', () => {
    expect(isThreadStart({ id: '6', conversation_id: '5' })).toBe(false);
  });
});

describe('orderThreadChronologically', () => {
  it('sortiert aufsteigend nach created_at', () => {
    const out = orderThreadChronologically([
      { id: 'b', created_at: '2026-01-02T00:00:00Z' },
      { id: 'a', created_at: '2026-01-01T00:00:00Z' },
    ]);
    expect(out.map((t) => t.id)).toEqual(['a', 'b']);
  });
});

describe('formatDump', () => {
  it('enthält Autor, ID und Text', () => {
    const md = formatDump({
      tweet: { id: '20', text: 'hallo welt', created_at: '2026-07-03T00:00:00Z' },
      author: { name: 'Thariq', username: 'trq212' },
      media: [],
      thread: null,
    });
    expect(md).toContain('Thariq');
    expect(md).toContain('@trq212');
    expect(md).toContain('20');
    expect(md).toContain('hallo welt');
  });
  it('rendert Thread-Posts nummeriert', () => {
    const md = formatDump({
      tweet: { id: '1', text: 'erster' },
      author: { name: 'A', username: 'a' },
      media: [],
      thread: [{ id: '1', text: 'erster' }, { id: '2', text: 'zweiter' }],
    });
    expect(md).toContain('## Thread');
    expect(md).toContain('1/2');
    expect(md).toContain('zweiter');
  });
});
```

- [ ] **Step 2: Tests laufen lassen (neue müssen fehlschlagen)**

Run:
```bash
npx vitest run scripts/lib/x-ingest.test.ts
```
Expected: FAIL — neue Funktionen undefiniert.

- [ ] **Step 3: Implementierung ergänzen**

An `scripts/lib/x-ingest.mjs` anhängen:
```javascript
export function extractTweetText(tweet) {
  return tweet?.note_tweet?.text ?? tweet?.text ?? '';
}

export function extractLinks(tweet) {
  const entities = tweet?.note_tweet?.entities ?? tweet?.entities ?? {};
  const urls = Array.isArray(entities.urls) ? entities.urls : [];
  const seen = new Set();
  const links = [];
  for (const u of urls) {
    const expanded = u.expanded_url ?? u.url;
    if (!expanded || seen.has(expanded)) continue;
    seen.add(expanded);
    links.push({ url: expanded, display: u.display_url ?? expanded });
  }
  return links;
}

export function extractMedia(mediaList) {
  if (!Array.isArray(mediaList)) return [];
  return mediaList.map((m) => ({
    type: m.type ?? 'unknown',
    alt: m.alt_text ?? '',
    url: m.url ?? m.preview_image_url ?? '',
  }));
}

export function isThreadStart(tweet) {
  return Boolean(tweet?.id && tweet.id === tweet.conversation_id);
}

export function orderThreadChronologically(tweets) {
  return [...tweets].sort((a, b) => {
    const ta = a?.created_at ? Date.parse(a.created_at) : 0;
    const tb = b?.created_at ? Date.parse(b.created_at) : 0;
    return ta - tb;
  });
}

export function formatDump({ tweet, author, media, thread }) {
  const handle = author?.username ? `@${author.username}` : 'unbekannt';
  const name = author?.name ?? 'Unbekannt';
  const lines = [`# X-Ingest: ${name} (${handle})`, ''];
  lines.push(`- **Tweet-ID:** ${tweet.id}`);
  lines.push(`- **URL:** https://x.com/${author?.username ?? 'i'}/status/${tweet.id}`);
  if (tweet.created_at) lines.push(`- **Datum:** ${tweet.created_at}`);
  const m = tweet.public_metrics;
  if (m) {
    lines.push(
      `- **Metriken:** ${m.like_count ?? 0} Likes · ${m.retweet_count ?? 0} Retweets · ${m.reply_count ?? 0} Replies · ${m.impression_count ?? '?'} Views`,
    );
  }
  lines.push('');
  if (tweet.article?.title) {
    lines.push(
      `> ⚠️ Nativer X-Artikel „${tweet.article.title}" — Volltext ggf. nicht über die API verfügbar; bei Bedarf Sekundärquelle prüfen.`,
      '',
    );
  }
  lines.push('## Text', '', extractTweetText(tweet), '');
  const links = extractLinks(tweet);
  if (links.length) {
    lines.push('## Links', '');
    for (const l of links) lines.push(`- [${l.display}](${l.url})`);
    lines.push('');
  }
  if (media.length) {
    lines.push('## Media', '');
    for (const md of media) {
      lines.push(`- **${md.type}**${md.alt ? `: ${md.alt}` : ''}${md.url ? ` (${md.url})` : ''}`);
    }
    lines.push('');
  }
  if (thread && thread.length) {
    lines.push('## Thread', '');
    thread.forEach((t, i) => {
      lines.push(`### ${i + 1}/${thread.length}`, '', extractTweetText(t), '');
    });
  }
  return lines.join('\n');
}
```

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run:
```bash
npx vitest run scripts/lib/x-ingest.test.ts
```
Expected: PASS (alle Tests grün).

- [ ] **Step 5: Commit**

```bash
git add scripts/lib/x-ingest.mjs scripts/lib/x-ingest.test.ts
git commit -m "feat(ingest): add content extraction + dump formatting with tests"
```

---

## Task 4: X-API-Client-Modul

**Files:**
- Create: `scripts/lib/x-client.mjs`

**Interfaces:**
- Consumes: `twitter-api-v2` (`TwitterApi`).
- Produces:
  - `createClient(bearerToken: string): TwitterApi`
  - `fetchTweet(client, id): Promise<{ data, includes, errors }>`
  - `resolveThreadForward(client, tweet, username): Promise<{ tweets, includes, method } | null>`
  - `resolveThreadBackward(client, tweet, maxTweets): Promise<tweet[]>` (newest→oldest)

**Hinweis:** Netzwerkcode wird bewusst nicht unit-getestet (Pay-per-use API); Verifikation erfolgt per echtem Lauf in Task 6. Der Reviewer-Gate ist die Code-Korrektheit.

- [ ] **Step 1: Client-Modul anlegen**

Create `scripts/lib/x-client.mjs`:
```javascript
import { TwitterApi } from 'twitter-api-v2';

export const TWEET_FIELDS = [
  'note_tweet',
  'article',
  'created_at',
  'public_metrics',
  'entities',
  'author_id',
  'conversation_id',
  'referenced_tweets',
  'attachments',
];
export const EXPANSIONS = ['author_id', 'referenced_tweets.id', 'attachments.media_keys'];
export const USER_FIELDS = ['name', 'username', 'verified'];
export const MEDIA_FIELDS = ['type', 'alt_text', 'url', 'preview_image_url'];

function queryOptions() {
  return {
    'tweet.fields': TWEET_FIELDS,
    expansions: EXPANSIONS,
    'user.fields': USER_FIELDS,
    'media.fields': MEDIA_FIELDS,
  };
}

export function createClient(bearerToken) {
  return new TwitterApi(bearerToken);
}

export async function fetchTweet(client, id) {
  return client.v2.singleTweet(id, queryOptions());
}

// Fall A: URL zeigt auf Thread-Start. Kanonisch conversation_id-Search (recent),
// Fallback Full-Archive. Je Weg nur die erste Seite (<=100) → kostenkontrolliert.
export async function resolveThreadForward(client, tweet, username) {
  const query = `conversation_id:${tweet.conversation_id} from:${username}`;
  const options = { ...queryOptions(), max_results: 100 };

  try {
    const recent = await client.v2.search(query, options);
    if (recent.tweets?.length) {
      return { tweets: recent.tweets, includes: recent.includes, method: 'recent-search' };
    }
  } catch {
    // Recent Search nicht verfügbar → Full-Archive versuchen
  }

  try {
    const all = await client.v2.searchAll(query, options);
    if (all.tweets?.length) {
      return { tweets: all.tweets, includes: all.includes, method: 'full-archive-search' };
    }
  } catch {
    // Kein Full-Archive-Zugang
  }

  return null;
}

// Fall B: URL zeigt auf späteren Tweet. Rückwärts der replied_to-Kette folgen,
// ein Request pro Tweet, gedeckelt auf maxTweets.
export async function resolveThreadBackward(client, tweet, maxTweets) {
  const chain = [tweet];
  let current = tweet;
  while (chain.length < maxTweets) {
    const replied = (current.referenced_tweets ?? []).find((r) => r.type === 'replied_to');
    if (!replied) break;
    const res = await fetchTweet(client, replied.id);
    if (!res?.data) break;
    chain.push(res.data);
    current = res.data;
  }
  return chain;
}
```

- [ ] **Step 2: Modul lädt fehlerfrei (Smoke-Check)**

Run:
```bash
node -e "import('./scripts/lib/x-client.mjs').then((m) => console.log(Object.keys(m).join(',')))"
```
Expected: Ausgabe `TWEET_FIELDS,EXPANSIONS,USER_FIELDS,MEDIA_FIELDS,createClient,fetchTweet,resolveThreadForward,resolveThreadBackward` (Reihenfolge egal, keine Import-Fehler).

- [ ] **Step 3: Commit**

```bash
git add scripts/lib/x-client.mjs
git commit -m "feat(ingest): add X API client (fetch tweet, thread cascade)"
```

---

## Task 5: CLI-Orchestrierung

**Files:**
- Modify: `scripts/ingest-x.mjs` (Skeleton ersetzen)

**Interfaces:**
- Consumes: alle Exports aus `scripts/lib/x-ingest.mjs` und `scripts/lib/x-client.mjs`.
- Produces: vollständiges CLI-Verhalten (Env, Cache, Fetch, Thread, Output, Fehler).

- [ ] **Step 1: `scripts/ingest-x.mjs` vollständig implementieren**

Ersetze den gesamten Inhalt von `scripts/ingest-x.mjs`:
```javascript
import fs from 'fs';
import path from 'path';
import {
  parseArgs,
  parseTweetId,
  extractMedia,
  extractTweetText,
  isThreadStart,
  orderThreadChronologically,
  formatDump,
} from './lib/x-ingest.mjs';
import {
  createClient,
  fetchTweet,
  resolveThreadForward,
  resolveThreadBackward,
} from './lib/x-client.mjs';

const rootDir = process.cwd();
const ingestDir = path.join(rootDir, 'scripts/.ingest');

function usage() {
  console.log(
    'Usage: npm run ingest:x -- <tweet-url-oder-id> [--thread] [--force] [--max-thread <n>]',
  );
}

function loadEnv() {
  try {
    process.loadEnvFile(path.join(rootDir, '.env.local'));
  } catch {
    // .env.local optional — Variable kann extern gesetzt sein
  }
}

function findAuthor(includes, authorId) {
  const users = includes?.users ?? [];
  return users.find((u) => u.id === authorId) ?? users[0] ?? null;
}

function formatApiError(err) {
  const code = err?.code ?? err?.data?.status;
  if (code === 401) return '✖ 401 Unauthorized — Bearer Token ungültig oder abgelaufen.';
  if (code === 403) return '✖ 403 Forbidden — App fehlt die Berechtigung für diesen Endpoint/Tweet.';
  if (code === 404) return '✖ 404 — Tweet gelöscht, privat oder ID falsch.';
  if (code === 429) {
    const reset = err?.rateLimit?.reset
      ? ` Reset: ${new Date(err.rateLimit.reset * 1000).toLocaleTimeString()}`
      : '';
    return `✖ 429 Rate-Limit erreicht.${reset} Kein Auto-Retry (Pay-per-use).`;
  }
  return `✖ API-Fehler: ${err?.message ?? String(err)}`;
}

async function resolveThread(client, tweet, author, maxThread) {
  if (isThreadStart(tweet)) {
    if (!author?.username) return { thread: null, method: null };
    const fwd = await resolveThreadForward(client, tweet, author.username);
    if (fwd) return { thread: orderThreadChronologically(fwd.tweets), method: fwd.method };
    console.warn(
      '⚠ Thread über Search nicht auflösbar (>7 Tage & kein Full-Archive-Zugang). Für ältere Threads die URL des LETZTEN Tweets übergeben.',
    );
    return { thread: null, method: null };
  }
  const chain = await resolveThreadBackward(client, tweet, maxThread);
  if (chain.length >= maxThread) {
    console.warn(`⚠ Rückwärts-Walk bei --max-thread=${maxThread} gekappt.`);
  }
  return { thread: orderThreadChronologically(chain), method: 'backward-walk' };
}

async function main() {
  const args = parseArgs(process.argv.slice(2));
  if (!args.input) {
    usage();
    process.exit(1);
  }

  let id;
  try {
    id = parseTweetId(args.input);
  } catch (err) {
    console.error(`✖ ${err.message}`);
    process.exit(1);
  }

  fs.mkdirSync(ingestDir, { recursive: true });
  const jsonPath = path.join(ingestDir, `${id}.json`);
  const mdPath = path.join(ingestDir, `${id}.md`);

  let payload;
  if (fs.existsSync(jsonPath) && !args.force) {
    console.log(
      `● Cache-Treffer: ${path.relative(rootDir, jsonPath)} (kein API-Call). --force zum Neuladen.`,
    );
    payload = JSON.parse(fs.readFileSync(jsonPath, 'utf8'));
  } else {
    loadEnv();
    const token = process.env.X_BEARER_TOKEN;
    if (!token) {
      console.error(
        '✖ X_BEARER_TOKEN fehlt. Lege .env.local an mit:\n  X_BEARER_TOKEN=dein_bearer_token\n(Vorlage: .env.example)',
      );
      process.exit(1);
    }

    const client = createClient(token);
    let res;
    try {
      res = await fetchTweet(client, id);
    } catch (err) {
      console.error(formatApiError(err));
      process.exit(1);
    }
    if (!res?.data) {
      console.error('✖ Kein Tweet in der Antwort (gelöscht, privat oder ID falsch?).');
      process.exit(1);
    }

    const tweet = res.data;
    const author = findAuthor(res.includes, tweet.author_id);
    let thread = null;
    let threadMethod = null;
    if (args.thread) {
      ({ thread, method: threadMethod } = await resolveThread(client, tweet, author, args.maxThread));
    }

    payload = {
      fetchedAt: new Date().toISOString(),
      id,
      tweet,
      includes: res.includes ?? {},
      thread,
      threadMethod,
    };
    fs.writeFileSync(jsonPath, JSON.stringify(payload, null, 2), 'utf8');
  }

  const tweet = payload.tweet;
  const author = findAuthor(payload.includes, tweet.author_id);
  const media = extractMedia(payload.includes?.media);
  const md = formatDump({ tweet, author, media, thread: payload.thread });
  fs.writeFileSync(mdPath, md, 'utf8');

  const text = extractTweetText(tweet);
  const threadNote = payload.thread
    ? ` · Thread: ${payload.thread.length} Posts (${payload.threadMethod})`
    : '';
  console.log(`✔ Dump: ${path.relative(rootDir, mdPath)}`);
  console.log(
    `  Autor: ${author?.name ?? '?'} (@${author?.username ?? '?'}) · ${text.length} Zeichen${threadNote}`,
  );
}

main().catch((err) => {
  console.error(`✖ Unerwarteter Fehler: ${err?.message ?? err}`);
  process.exit(1);
});
```

- [ ] **Step 2: Fehlermeldung ohne Token/Cache verifizieren**

Vorbereitung: sicherstellen, dass keine `.env.local` mit Token existiert UND kein Cache für die Test-ID. Dann:
```bash
X_BEARER_TOKEN= node scripts/ingest-x.mjs 999 --force
```
Expected: Meldung „✖ X_BEARER_TOKEN fehlt…", Exit-Code 1. (Falls lokal eine echte `.env.local` existiert, wird stattdessen ein echter Call versucht — dann diesen Schritt überspringen und in Task 6 verifizieren.)

- [ ] **Step 3: ID-Parse-Fehler verifizieren**

```bash
node scripts/ingest-x.mjs "https://x.com/user"
```
Expected: Meldung „✖ Konnte keine Tweet-ID …", Exit-Code 1 (kein API-Call, da Parsing vor Env/Fetch).

- [ ] **Step 4: Commit**

```bash
git add scripts/ingest-x.mjs
git commit -m "feat(ingest): wire up CLI orchestration (cache, fetch, thread, output)"
```

---

## Task 6: Echter API-Lauf zur Verifikation

**Files:** keine Änderung — reine Verifikation. Erfordert gültigen `X_BEARER_TOKEN`.

**Interfaces:** keine.

- [ ] **Step 1: `.env.local` mit Token anlegen (falls noch nicht vorhanden)**

Datei `.env.local` im Projekt-Root (NICHT committen — ist gitignored):
```
X_BEARER_TOKEN=<dein echter Bearer Token aus vibedeck-ingest>
```

- [ ] **Step 2: Einzel-Tweet abrufen**

```bash
npm run ingest:x -- https://x.com/trq212/status/2073100352921215386
```
Expected: `scripts/.ingest/2073100352921215386.json` und `.md` entstehen; Konsole zeigt Autor „Thariq" und Zeichenzahl. `scripts/.ingest/2073100352921215386.md` öffnen und prüfen: Autor, Datum, Text/Metadaten vorhanden.

- [ ] **Step 3: Cache-Hit verifizieren**

Denselben Befehl erneut ausführen:
```bash
npm run ingest:x -- https://x.com/trq212/status/2073100352921215386
```
Expected: „● Cache-Treffer …" erscheint, KEIN neuer API-Call.

- [ ] **Step 4: Thread verifizieren**

Für einen frischen Thread (≤7 Tage), URL des ersten Tweets:
```bash
npm run ingest:x -- <url-eines-frischen-threads> --thread
```
Expected: Konsole meldet „Thread: N Posts (recent-search)"; Dump enthält Abschnitt `## Thread` mit nummerierten Posts in chronologischer Reihenfolge.

Für einen älteren Thread die URL des LETZTEN Tweets nutzen:
```bash
npm run ingest:x -- <url-des-letzten-tweets> --thread
```
Expected: „Thread: N Posts (backward-walk)"; Posts chronologisch im Dump.

- [ ] **Step 5: Bestätigen, dass nichts Sensibles staged wird**

```bash
git status --short
```
Expected: weder `.env.local` noch `scripts/.ingest/` erscheinen (beide gitignored).

---

## Task 7: CLAUDE.md-Workflow dokumentieren

**Files:**
- Modify: `CLAUDE.md`

**Interfaces:** keine.

- [ ] **Step 1: Abschnitt ergänzen**

In `CLAUDE.md` nach dem Abschnitt „### Working with Prompts" (oder am sinnvollen Ort im Bereich „Common Patterns") einfügen:
```markdown
### Knowledge-Artikel aus X-Posts

Wenn ein Knowledge-Artikel aus einer X-URL angelegt werden soll:

1. `npm run ingest:x -- <url>` ausführen. Bei Threads `--thread` ergänzen; ist der Thread älter als ~7 Tage, die URL des **letzten** Tweets verwenden (Rückwärts-Auflösung ohne Search-Endpoint).
2. Dump aus `scripts/.ingest/<id>.md` lesen (Roh-JSON liegt daneben als `<id>.json`).
3. Artikel nach dem Frontmatter-Schema (`src/types/knowledge.ts`) in `src/content/knowledge/` schreiben; `sourceURL`, `author`, `sourceDate` aus dem Dump übernehmen.
4. `npm run content:generate-search-index` ausführen.

Voraussetzung: `X_BEARER_TOKEN` in `.env.local` (Vorlage: `.env.example`). Ersetzt den früheren fxtwitter-Workaround.
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude): document X-ingest workflow for knowledge articles"
```

---

## Self-Review

**Spec coverage:**
- Dependency `twitter-api-v2` devDep → Task 1. ✓
- Script `scripts/ingest-x.mjs` + `ingest:x` → Tasks 1, 5. ✓
- Secret nur `X_BEARER_TOKEN` via `.env.local`, `.env.example` committed → Tasks 1, 5. ✓
- Cache `scripts/.ingest/`, gitignored → Tasks 1, 5. ✓
- Ein API-Call `singleTweet` mit Feldern/Expansions → Task 4. ✓
- Thread-Kaskade (recent → searchAll → backward-walk), `--max-thread` Guard → Tasks 4, 5. ✓
- Output JSON + MD-Dump (Autor, Datum, Metriken, Volltext via note_tweet, Links, Media, Thread) → Tasks 3, 5. ✓
- Fehlerbehandlung (fehlender Token, 401/403/404/429, Artikel-Metadaten, Thread-Grenzen) → Tasks 3, 5. ✓
- CLAUDE.md-Workflow → Task 7. ✓
- Verifikation Real-Run (Einzel, Cache, Thread) → Task 6. ✓

**Placeholder scan:** Keine TBD/TODO; jede Code-Änderung zeigt vollständigen Code. ✓

**Type consistency:** `parseArgs` liefert `{ input, thread, force, maxThread }` — genau so in Task 5 konsumiert. `resolveThreadForward` liefert `{ tweets, includes, method }` — in `resolveThread` (Task 5) als `fwd.tweets`/`fwd.method` genutzt. `formatDump({ tweet, author, media, thread })` — Aufruf in Task 5 identisch. `fetchTweet` liefert `{ data, includes }` — konsistent verwendet. ✓

**Korrektur ggü. Spec:** Der Search-Query lautet `conversation_id:<id> from:<username>` **ohne** `-is:reply` — der Zusatz aus dem Spec-Entwurf würde die Thread-Fortsetzungen (Self-Replies) fälschlich ausschließen. Spec wird entsprechend korrigiert.
